# WSL2 Deployment - Migration Summary

## 📊 Обзор миграции на WSL2

**Дата:** January 31, 2026  
**Статус:** ✅ Готово к использованию

### Что изменилось

#### До (Legacy)
```
Windows → SSH → Production Server → Docker Build → Deploy
```
- Сборка образа на production сервере
- Медленная (SSH overhead)
- Сложно инспектировать образ
- Нельзя протестировать до деплоя

#### После (WSL2)
```
Windows → WSL2 → Docker Build → Save → Transfer → Production → Deploy
```
- Сборка образа локально в WSL2
- Быстрая (локальный кеш)
- Легко инспектировать образ
- Можно тестировать локально

### Ключевые преимущества

| Функция | Legacy | WSL2 | Улучшение |
|---------|--------|------|-----------|
| Скорость сборки (повторная) | 2-3 мин | 30-60 сек | ⚡ 3-4x быстрее |
| Прозрачность образа | ❌ Низкая | ✅ Высокая | 🔍 Полный контроль |
| Локальное тестирование | ❌ Сложно | ✅ Просто | 🧪 Один клик |
| Инспекция файлов | ❌ Нет | ✅ Да | 👀 Точно видим что попало |
| Нагрузка на production | ⚠️ Высокая | ✅ Низкая | 🎯 Только деплой |
| Отладка | ❌ Сложная | ✅ Легкая | 🐛 Breakpoints работают |

## 📁 Новые файлы

### Документация
- ✅ `WSL2_DEPLOYMENT.md` - полная документация WSL2 деплоя
- ✅ `WSL2_QUICKSTART.md` - быстрый старт и шпаргалка
- ✅ `WSL2_TECHNICAL.md` - технические детали
- ✅ `WSL2_EXAMPLES.md` - примеры использования
- ✅ `WSL2_MIGRATION_SUMMARY.md` - этот файл

### Скрипты
- ✅ `scripts/wsl2-deploy.ps1` - основной скрипт деплоя

### Обновленные файлы
- ✅ `agents.md` - добавлена секция "WSL2 + Docker деплой"
- ✅ `README.md` - обновлены ссылки на новую документацию

## 🚀 Команды для агентов

### Рекомендуемый workflow

```powershell
# 1. Разработка
.\scripts\test-mode.ps1 start

# 2. Проверка безопасности
python scripts\check-production-safety.py

# 3. Полный деплой
.\scripts\wsl2-deploy.ps1 full
```

### Быстрые команды

```powershell
# Только сборка и деплой (для быстрых фиксов)
.\scripts\wsl2-deploy.ps1 build
.\scripts\wsl2-deploy.ps1 deploy -Force

# Проверка что попало в образ
.\scripts\wsl2-deploy.ps1 inspect

# Локальное тестирование
.\scripts\wsl2-deploy.ps1 test

# Статус production
.\scripts\wsl2-deploy.ps1 status
```

## 📚 Документация для агентов

### Основные файлы (по приоритету)

1. **[agents.md](agents.md)** - главный файл для AI агентов
   - Секция "WSL2 + Docker деплой" - основной workflow
   - Секция "Миграции" - работа с БД
   - Секция "Production безопасность" - важно!

2. **[WSL2_QUICKSTART.md](WSL2_QUICKSTART.md)** - быстрый старт
   - Команды для копипасты
   - Чеклист перед деплоем
   - Troubleshooting

3. **[WSL2_EXAMPLES.md](WSL2_EXAMPLES.md)** - примеры
   - 22+ практических сценария
   - Шпаргалка команд

4. **[WSL2_DEPLOYMENT.md](WSL2_DEPLOYMENT.md)** - полная документация
   - Детальное описание процесса
   - Best practices
   - Сравнение с legacy

5. **[WSL2_TECHNICAL.md](WSL2_TECHNICAL.md)** - техническая информация
   - Архитектура системы
   - Диагностика проблем

## ⚙️ Настройка WSL2 (первый раз)

```powershell
# Установка WSL2
wsl --install -d Ubuntu-22.04

# Настройка Docker в WSL2
wsl -d Ubuntu-22.04
sudo apt update && sudo apt install -y docker.io
sudo usermod -aG docker $USER
sudo service docker start

# Auto-start Docker
sudo nano /etc/wsl.conf
# [boot]
# command = "service docker start"

# Перезапуск WSL
exit
wsl --shutdown

# Проверка
.\scripts\wsl2-deploy.ps1 check-wsl
```

## 🎯 Типичные задачи агентов

### Задача 1: Исправить баг в коде

```powershell
# 1. Исправить код в VS Code
# 2. Задеплоить
.\scripts\wsl2-deploy.ps1 build
.\scripts\wsl2-deploy.ps1 deploy -Force
# 3. Проверить
.\scripts\wsl2-deploy.ps1 status
```

### Задача 2: Добавить новую фичу

```powershell
# 1. Разработка в test mode
.\scripts\test-mode.ps1 start

# 2. После разработки - полный цикл
.\scripts\wsl2-deploy.ps1 full
```

### Задача 3: Проверить что файл попал в образ

```powershell
# Собрать и проинспектировать
.\scripts\wsl2-deploy.ps1 build
.\scripts\wsl2-deploy.ps1 inspect

# Или проверить конкретный файл
wsl -d Ubuntu-22.04 -e bash -c "sudo docker run --rm carebot:latest ls -la /app/CareBot/ | grep handlers.py"
```

### Задача 4: Обновить миграции

```powershell
# Создать миграцию: CareBot/CareBot/migrations/009_*.py
.\scripts\wsl2-deploy.ps1 migrations
.\scripts\wsl2-deploy.ps1 apply-migrations
```

### Задача 5: Проблема с production

```powershell
# Проверка
.\scripts\wsl2-deploy.ps1 status
.\scripts\wsl2-deploy.ps1 logs

# Перезапуск если нужно
.\scripts\wsl2-deploy.ps1 restart

# Откат если критично
# См. WSL2_EXAMPLES.md - сценарий 17
```

## ⚠️ Важные моменты

### Безопасность

1. **ВСЕГДА** запускать production safety check:
   ```powershell
   python scripts\check-production-safety.py
   ```

2. **НИКОГДА** не включать тестовые файлы в образ:
   - `mock_sqlite_helper.py`
   - `test_*.py`

3. **Автоматические бэкапы** создаются при деплое

### Миграции

- Миграции НЕ в образе (монтируются как volume)
- Можно обновлять миграции без пересборки образа
- Команды: `migrations`, `apply-migrations`, `migration-status`

### Volumes

Что НЕ в образе (монтируется):
- База данных: `./data:/app/data`
- Миграции: `./migrations:/app/CareBot/migrations`

## 🔄 Миграция с Legacy

### Если используете старый workflow

```powershell
# Последний бэкап старым способом
.\scripts\update-production.ps1 backup

# Переход на новый
.\scripts\wsl2-deploy.ps1 full

# Проверка
.\scripts\wsl2-deploy.ps1 status
```

### Параллельное использование

Можно использовать оба способа:
- Legacy для emergency fixes (если WSL2 недоступен)
- WSL2 для обычных деплоев

Оба скрипта создают бэкапы и работают с одним production сервером.

## 📊 Метрики и мониторинг

### Health checks

```powershell
# Локальный тест
curl http://localhost:5556/health

# Production
curl http://192.168.1.125:5555/health

# Через скрипт
.\scripts\wsl2-deploy.ps1 status
```

### Логи

```powershell
# Последние 50 строк
.\scripts\wsl2-deploy.ps1 logs

# Следить за логами (tail -f)
ssh ubuntu@192.168.1.125 "cd /home/ubuntu/carebot-production && docker compose logs --follow"
```

## 🎓 Обучение агентов

### Что должен знать агент

1. **Основной workflow**: `full` команда для полного деплоя
2. **Инспекция образа**: `inspect` для проверки содержимого
3. **Локальное тестирование**: `test` перед деплоем
4. **Production safety**: обязательная проверка перед сборкой
5. **Миграции**: отдельная синхронизация без пересборки

### Где искать информацию

- Краткая справка: `.\scripts\wsl2-deploy.ps1` (без параметров)
- Быстрый старт: [WSL2_QUICKSTART.md](WSL2_QUICKSTART.md)
- Примеры: [WSL2_EXAMPLES.md](WSL2_EXAMPLES.md)
- Детали: [WSL2_DEPLOYMENT.md](WSL2_DEPLOYMENT.md)
- Технические вопросы: [WSL2_TECHNICAL.md](WSL2_TECHNICAL.md)

## ✅ Чеклист готовности

### WSL2 настроен
- [ ] WSL2 установлен (`wsl --version`)
- [ ] Ubuntu 22.04 установлен (`wsl -l -v`)
- [ ] Docker в WSL2 работает (`.\scripts\wsl2-deploy.ps1 check-wsl`)
- [ ] Auto-start настроен (`/etc/wsl.conf`)

### Документация изучена
- [ ] [agents.md](agents.md) - секция WSL2
- [ ] [WSL2_QUICKSTART.md](WSL2_QUICKSTART.md) - команды
- [ ] [WSL2_EXAMPLES.md](WSL2_EXAMPLES.md) - примеры

### Первый деплой
- [ ] Production safety check пройден
- [ ] Образ собран (`build`)
- [ ] Образ проинспектирован (`inspect`)
- [ ] Локальный тест пройден (`test`)
- [ ] Production деплой выполнен (`deploy`)
- [ ] Health check OK (`status`)

## 🎯 Next Steps

1. **Настроить WSL2** (если еще не настроено)
2. **Прочитать WSL2_QUICKSTART.md**
3. **Сделать тестовый деплой**: `.\scripts\wsl2-deploy.ps1 full`
4. **Использовать WSL2 как основной способ деплоя**

---

**Legacy workflow** (update-production.ps1) остается доступным но считается устаревшим.

**Вопросы?** См. документацию или используйте `.\scripts\wsl2-deploy.ps1` для справки.

**Дата:** January 31, 2026
