# VS Code Quick Start для CareBot

## 🚀 Запуск тестового режима по F5

### ⚡ Первый запуск (установка зависимостей)

1. **Откройте проект** в VS Code
2. **Нажмите F5** или откройте палитру команд (`Ctrl+Shift+P`) и выберите "Debug: Start Debugging"
3. При первом запуске автоматически установятся зависимости из `requirements.txt`
4. Выберите **"🧪 CareBot Test Mode"** из списка конфигураций
5. Бот запустится в тестовом режиме с mock базой данных

### 📦 Ручная установка зависимостей (если нужно)

```powershell
cd CareBot
pip install -r requirements.txt
```

Или через VS Code:
- `Ctrl+Shift+P` → "Tasks: Run Task" → "Install Dependencies"

## 🎯 Доступные конфигурации запуска

| Конфигурация | Описание | Горячая клавиша |
|-------------|----------|----------------|
| 🧪 CareBot Test Mode | Локальный тестовый режим с mock БД | F5 (по умолчанию) |
| 🚀 CareBot Production Mode | Локальный запуск с реальной БД | F5 → выбрать |
| 🔧 CareBot Hybrid Mode | Запуск hybrid версии | F5 → выбрать |

## ⚡ Tasks (Ctrl+Shift+P → "Tasks: Run Task")

| Task | Описание |
|------|----------|
| Install Dependencies | Установить Python зависимости |
| 🧪 Start Test Mode | Запустить тестовый режим через скрипт |
| ⏹️ Stop Test Mode | Остановить процессы тестового режима |
| 📊 Test Mode Status | Проверить статус тестового режима |
| 🚀 Deploy to Production | Полный деплой на production |
| 🔄 Sync Migrations Only | Синхронизировать только миграции |

## 🔍 Отладка

### Breakpoints
- Устанавливайте breakpoints в коде (F9)
- В тестовом режиме все остановки будут работать
- Используйте Debug Console для выполнения команд

### Логирование
- В тестовом режиме все mock операции логируются с префиксом `🧪 Mock:`
- Production логи выводятся в Debug Console
- Интегрированный терминал показывает все output

## 📁 Структура workspace

```
Care/
├── .vscode/
│   ├── launch.json      # Конфигурации запуска (F5)
│   ├── settings.json    # Настройки VS Code
│   └── tasks.json       # Автоматизированные задачи
├── CareBot/
│   ├── run_bot.py       # Основной entry point
│   └── CareBot/
│       ├── config.py    # Конфигурация с TEST_MODE
│       ├── mock_sqlite_helper.py  # Mock база данных
│       └── ...
└── scripts/
    ├── test-mode.ps1    # Управление локальным тестом
    └── update-production.ps1  # Деплой
```

## 🛠️ Рекомендуемые расширения

VS Code автоматически предложит установить:
- Python
- Python Debugger (debugpy)
- PowerShell
- SQLTools
- SQLite

## 💡 Tips & Tricks

### Быстрое переключение между режимами
1. **F5** → тестовый режим (отладка)
2. **Ctrl+Shift+P** → "Tasks: Run Task" → "🧪 Start Test Mode" (через скрипт)
3. **Ctrl+F5** → запуск без отладки

### Мониторинг тестового режима
- **Ctrl+Shift+P** → "Tasks: Run Task" → "📊 Test Mode Status"
- Проверяет активные процессы и конфигурацию

### Быстрый деплой
- **Ctrl+Shift+P** → "Tasks: Run Task" → "🚀 Deploy to Production"
- Полностью автоматический деплой с бэкапом

### SQLite Database
- Используйте расширение SQLite для просмотра БД
- В тестовом режиме база данных mock (в памяти)
- В production режиме база в `CareBot/CareBot/db/database.db`

## ⚠️ Важные замечания

- **Тестовый режим** - данные НЕ сохраняются, mock в памяти
- **Production режим** - будет использовать реальную базу данных
- Всегда проверяйте переменную `CAREBOT_TEST_MODE` в Debug Console
- Для production деплоя используйте tasks или скрипты

## 🆘 Troubleshooting

### F5 не работает
- Убедитесь что установлено расширение Python Debugger
- Проверьте что `run_bot.py` существует
- Откройте любой .py файл в workspace

### ModuleNotFoundError (flask, aiosqlite, etc.)
```
ModuleNotFoundError: No module named 'flask'
```
**Решение:** 
1. `Ctrl+Shift+P` → "Tasks: Run Task" → "Install Dependencies"
2. Или вручную: `cd CareBot && pip install -r requirements.txt`
3. Перезапустите отладку (F5)

### Mock helper не найден
- Убедитесь что файл `CareBot/CareBot/mock_sqlite_helper.py` создан
- Перезапустите VS Code

### Python не найден
- Установите Python и добавьте в PATH
- Или настройте `python.defaultInterpreterPath` в settings.json

### Зависимости не устанавливаются автоматически
- Запустите вручную: `cd CareBot && pip install -r requirements.txt`
- Проверьте что pip работает: `pip --version`