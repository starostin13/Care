# CareBot Local Test Mode Guide

## Обзор локального тестового режима

CareBot поддерживает специальный локальный тестовый режим, который позволяет запускать бота для отладки без подключения к реальной базе данных. Этот режим запускается как обычный Python процесс на локальной машине, а не в Docker контейнере.

## Ключевые особенности

### 🧪 Mock Database
- Все операции с базой данных эмулируются в памяти
- Данные не сохраняются между сессиями
- Предустановленные тестовые пользователи и альянсы
- Все операции логируются с префиксом `🧪 Mock:`

### 🤖 Отдельный Telegram Bot
- Использует отдельный test token: `8270355955:AAGm2RuyzbUO_2p4lhXwkOtolkrcgshuZ-4`
- Не влияет на production бота
- Полностью изолированная среда

### 🐍 Локальный Python процесс
- Запускается напрямую через Python (не Docker)
- Логи видны прямо в консоли
- Легкая отладка и разработка
- Быстрый перезапуск

## Быстрый старт

### Запуск локального тестового режима
```powershell
# Запустить тестовый бот локально
.\scripts\test-mode.ps1 start

# Проверить статус
.\scripts\test-mode.ps1 status

# Показать debug информацию
.\scripts\test-mode.ps1 debug
```

### Остановка
```powershell
# Остановить локальные процессы
.\scripts\test-mode.ps1 stop

# Или просто Ctrl+C в консоли где запущен бот
```

## Конфигурация

### Переменные окружения
- `CAREBOT_TEST_MODE=true` - автоматически устанавливается скриптом
- `TELEGRAM_BOT_TOKEN_TEST` - токен тестового бота (загружается из .env файла)
- `TELEGRAM_BOT_TOKEN` - токен production бота (загружается из .env файла)

### Конфигурация токенов (ВАЖНО!)

**Токены НЕ должны быть в коде!** Все токены хранятся в `.env` файле:

1. **Создайте .env файл** (уже создан, но если его нет):
```bash
cp .env.example .env
```

2. **Заполните токены** в `.env`:
```dotenv
# Production токен от @BotFather
TELEGRAM_BOT_TOKEN=your_production_token_here

# Тестовый токен от @BotFather  
TELEGRAM_BOT_TOKEN_TEST=your_test_token_here
```

3. **Безопасность**:
   - ✅ `.env` исключен из Git через `.gitignore`
   - ✅ `.env` исключен из Docker через `.dockerignore`  
   - ✅ `.env` блокируется системой production безопасности

### Файлы конфигурации
- `scripts/test-mode.ps1` - скрипт управления локальным тестовым режимом
- `mock_sqlite_helper.py` - mock версия базы данных
- Все `*_helper.py` файлы автоматически переключаются на mock

### Требования
- Python 3.x в PATH
- Все зависимости проекта установлены
- Правильная структура каталогов CareBot

## Mock данные

### Предустановленные пользователи
```python
# TestUser1 - администратор
telegram_id: '325313837'
nickname: 'TestUser1'
alliance: 1 (Crimson Legion)
is_admin: True

# TestUser2 - обычный пользователь  
telegram_id: '123456789'
nickname: 'TestUser2'
alliance: 2 (Shadow Pact)
is_admin: False
```

### Альянсы
- Crimson Legion (red)
- Shadow Pact (black) 
- Iron Brotherhood (gray)
- Storm Guard (blue)
- Void Seekers (purple)

### Mock функции
Все функции из `sqllite_helper.py` эмулируются:
- `get_user_by_telegram_id()`
- `save_mission()`
- `get_alliance_by_id()`
- `get_localized_text()`
- И многие другие...

## Разработка

### Добавление новых mock функций
Редактируйте `CareBot/CareBot/mock_sqlite_helper.py`:

```python
async def new_function(param):
    print(f"🧪 Mock: new_function({param})")
    # Вернуть тестовые данные
    return {"status": "mock_success"}
```

### Отладка
Все mock функции выводят отладочную информацию с префиксом `🧪 Mock:`

### Автоматическое переключение
Система автоматически определяет режим через `config.TEST_MODE` и подключает соответствующий helper:

```python
# В любом *_helper.py файле
if config.TEST_MODE:
    import mock_sqlite_helper as sqllite_helper
    print("🧪 Helper using MOCK SQLite helper")
else:
    import sqllite_helper
    print("✅ Helper using REAL SQLite helper")
```

## Команды управления

| Команда | Описание |
|---------|----------|
| `start` | Запустить локальный тестовый режим |
| `stop` | Остановить локальные Python процессы |
| `status` | Проверить статус и переменные окружения |
| `debug` | Показать подробную debug информацию |

## Локальная разработка

### Преимущества локального режима
- ✅ Быстрый запуск без Docker
- ✅ Логи прямо в консоли
- ✅ Легкая отладка с breakpoints
- ✅ Мгновенный перезапуск
- ✅ Не требует сборки образа

### Workflow для разработчиков

1. **Разработка новой функции:**
   ```powershell
   .\scripts\test-mode.ps1 start
   # Работайте с кодом, логи видны в консоли
   # Ctrl+C для остановки
   ```

2. **Отладка проблем:**
   ```powershell
   .\scripts\test-mode.ps1 debug
   # Проверьте debug информацию
   ```

3. **Тестирование изменений:**
   ```powershell
   # Остановить: Ctrl+C
   # Запустить снова: .\scripts\test-mode.ps1 start
   ```

4. **Деплой на production:**
   ```powershell
   .\scripts\update-production.ps1 update
   ```

## Troubleshooting

### Python не найден
```
❌ Python не найден в PATH!
```
**Решение:** Установите Python и добавьте в PATH, или используйте полный путь

### Mock helper не найден
```
❌ Файл mock_sqlite_helper.py не найден!
```
**Решение:** Убедитесь что файл создан в `CareBot\CareBot\mock_sqlite_helper.py`

### Telegram токен не найден
```
❌ TELEGRAM_BOT_TOKEN_TEST не найден
```
**Решение:** Убедитесь что токен настроен в `config.py`

### Процесс не останавливается
```
.\scripts\test-mode.ps1 stop
```
Или найдите процесс вручную:
```powershell
Get-Process -Name "python" | Where-Object { $_.CommandLine -like "*run_bot.py*" }
```

## Отличия от Production

| Аспект | Production | Local Test Mode |
|--------|------------|-----------------|
| База данных | SQLite файл | Mock в памяти |
| Telegram токен | Production | Test (8270355955:...) |
| Платформа | Docker | Локальный Python |
| Данные | Персистентные | Эфемерные |
| Логи | Docker logs | Консоль |
| Отладка | Сложная | Простая |

## Безопасность

⚠️ **Важно:**
- Локальный тестовый режим НЕ СОХРАНЯЕТ данные
- Используйте только для разработки и отладки
- Никогда не используйте production токен в тестах
- Тестовые данные существуют только во время работы процесса

---

*Этот документ должен обновляться при изменении тестовой инфраструктуры.*