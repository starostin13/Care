# CareBot - Deployment Guide

CareBot - это приложение для управления игровыми миссиями и битвами. Этот документ описывает процесс деплоя приложения через Docker на Ubuntu сервер.

## 🚀 Быстрый старт

### Предварительные требования

**На локальной машине (Windows):**
- Docker Desktop
- OpenSSH клиент (входит в Windows 10/11)
- PowerShell 5.1+

**На удаленном сервере (Ubuntu):**
- Docker и Docker Compose
- SSH сервер

### Настройка удаленного сервера

Подключитесь к серверу и установите зависимости:

```bash
ssh ubuntu@192.168.1.125

# Установка Docker
sudo apt update
sudo apt install -y docker.io docker-compose
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker ubuntu

# Перелогиньтесь или выполните:
newgrp docker
```

### Деплой приложения

1. **Склонируйте репозиторий:**
   ```powershell
   git clone <repository-url>
   cd Care
   ```

2. **Выполните деплой:**
   ```powershell
   .\scripts\deploy.ps1
   ```

   Скрипт автоматически:
   - Соберет Docker образ
   - Скопирует файлы на сервер
   - Запустит приложение в контейнере

3. **Проверьте статус:**
   ```powershell
   .\scripts\monitor.ps1 -Status
   ```

## 📊 Управление приложением

### Мониторинг

```powershell
# Статус контейнера и приложения
.\scripts\monitor.ps1 -Status

# Просмотр логов (последние 50 строк)
.\scripts\monitor.ps1 -Logs

# Просмотр логов (последние 100 строк)
.\scripts\monitor.ps1 -Logs -LogLines 100
```

### Управление службой

```powershell
# Перезапуск приложения
.\scripts\monitor.ps1 -Restart

# Остановка приложения
.\scripts\monitor.ps1 -Stop

# Запуск приложения
.\scripts\monitor.ps1 -Start
```

### Доступ к базе данных

```powershell
# Скопировать базу данных локально
.\scripts\db-access.ps1 -CopyDatabase

# Выполнить SQL запрос
.\scripts\db-access.ps1 -RunQuery "SELECT * FROM mission_stack LIMIT 5;"

# Посмотреть структуру таблиц
.\scripts\db-access.ps1 -RunQuery ".tables"
```

## 🔧 Конфигурация

### Переменные окружения

Скопируйте `.env.example` в `.env` и настройте:

```bash
# Конфигурация сервера
SERVER_HOST=0.0.0.0
SERVER_PORT=5555

# Путь к базе данных
DATABASE_PATH=/app/data/game_database.db

# Telegram Bot (если используется)
TELEGRAM_BOT_TOKEN=your_token_here
```

### Порты

- **5555** - основное приложение
- **8080** - админка базы данных (опционально)

### Персистентные данные

Данные сохраняются в директории `./data` на сервере:
- `game_database.db` - основная база данных
- Логи и другие файлы

## 🛠️ Разработка

### Локальная разработка

```powershell
# Запуск в режиме разработки
docker-compose -f docker-compose.dev.yml up

# Или без Docker
cd CareBot
pip install -r requirements.txt
python runserver.py
```

### Отладка

```powershell
# Подключение к контейнеру
ssh ubuntu@192.168.1.125
docker exec -it carebot-app bash

# Просмотр логов в реальном времени
ssh ubuntu@192.168.1.125 "docker logs -f carebot-app"
```

## 🗄️ Миграции базы данных

Миграции находятся в `CareBot/CareBot/database/migrations/`:

```
migrations/
├── 001_add_winner_bonus_column.sql
├── 001_migrate_winner_bonus.py
└── ...
```

### Применение миграций

```powershell
# Выполнить миграцию на сервере
ssh ubuntu@192.168.1.125 "cd /home/ubuntu/carebot && docker exec carebot-app python database/migrations/001_migrate_winner_bonus.py"
```

## 🚨 Экстренное восстановление

### Бэкап базы данных

```powershell
# Создать бэкап
scp ubuntu@192.168.1.125:/home/ubuntu/carebot/data/game_database.db ./backup_$(Get-Date -Format "yyyy-MM-dd_HH-mm").db
```

### Восстановление из бэкапа

```powershell
# Остановить приложение
.\scripts\monitor.ps1 -Stop

# Восстановить базу
scp ./backup_2023-10-21_15-30.db ubuntu@192.168.1.125:/home/ubuntu/carebot/data/game_database.db

# Запустить приложение
.\scripts\monitor.ps1 -Start
```

## 📞 Поддержка

### Полезные команды

```powershell
# Проверка подключения к серверу
ssh ubuntu@192.168.1.125 "echo 'Connection OK'"

# Статистика использования ресурсов
ssh ubuntu@192.168.1.125 "docker stats carebot-app --no-stream"

# Очистка старых образов Docker
ssh ubuntu@192.168.1.125 "docker system prune -f"
```

### Типичные проблемы

1. **Контейнер не запускается:**
   ```powershell
   .\scripts\monitor.ps1 -Logs
   ```

2. **База данных не доступна:**
   ```powershell
   .\scripts\db-access.ps1 -RunQuery ".tables"
   ```

3. **Приложение не отвечает:**
   ```powershell
   .\scripts\monitor.ps1 -Restart
   ```

---

Для получения дополнительной информации о структуре проекта см. `agents.md`.