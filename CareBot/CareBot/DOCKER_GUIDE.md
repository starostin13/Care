# 🐳 Docker развертывание CareBot

## 📋 Компоненты системы

### Контейнеры:
1. **carebot_webapp** - Flask веб-приложение + Mini App
2. **carebot_telegram** - Telegram Bot
3. **carebot_db** - PostgreSQL база данных
4. **carebot_redis** - Redis для кэширования
5. **carebot_nginx** - Nginx reverse proxy
6. **carebot_mobile_sync** - API для мобильных приложений

### Порты:
- **80** - HTTP (Nginx)
- **443** - HTTPS (Nginx, если настроен SSL)
- **5432** - PostgreSQL (только для разработки)
- **6379** - Redis (только для разработки)

## 🚀 Быстрый старт

### Windows:
```cmd
# 1. Клонируйте репозиторий
git clone <repository-url>
cd CareBot

# 2. Скопируйте и отредактируйте конфигурацию
copy .env.example .env
# Отредактируйте .env файл!

# 3. Запустите развертывание
deploy.bat
```

### Linux/macOS:
```bash
# 1. Клонируйте репозиторий  
git clone <repository-url>
cd CareBot

# 2. Скопируйте и отредактируйте конфигурацию
cp .env.example .env
# Отредактируйте .env файл!

# 3. Сделайте скрипт исполняемым и запустите
chmod +x deploy.sh
./deploy.sh
```

## ⚙️ Конфигурация

### Обязательные настройки в .env:

```bash
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Надежный пароль для базы данных
DB_PASSWORD=very_secure_password_here

# Секретный ключ для Flask (сгенерируйте случайно)
SECRET_KEY=your-very-secure-secret-key-here-32-chars-minimum
```

### Опциональные настройки:

```bash
# Домен для продакшена
DOMAIN=your-domain.com

# Настройки логирования
LOG_LEVEL=INFO

# Настройки карты
DEFAULT_MAP_SIZE=20
```

## 🔧 Управление контейнерами

### Основные команды:

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f telegram_bot

# Перезапуск конкретного сервиса
docker-compose restart webapp

# Проверка статуса
docker-compose ps

# Масштабирование сервиса
docker-compose up -d --scale webapp=2
```

### Обновление приложения:

```bash
# 1. Получить последние изменения
git pull

# 2. Пересобрать и запустить
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 🌐 URL-адреса сервисов

После успешного развертывания доступны:

- **📱 Mini App**: http://localhost/miniapp
- **🗺️ Карта**: http://localhost/map
- **🖨️ Станция печати**: http://localhost/print-station
- **🔧 API**: http://localhost/api/
- **💊 Health Check**: http://localhost/health

## 📊 Мониторинг

### Health Check:
```bash
curl http://localhost/health
```

### Логи в реальном времени:
```bash
# Все сервисы
docker-compose logs -f

# Только веб-приложение
docker-compose logs -f webapp

# Только Telegram бот
docker-compose logs -f telegram_bot

# Только база данных
docker-compose logs -f database
```

### Использование ресурсов:
```bash
docker stats
```

## 🔐 Безопасность

### Продакшен настройки:

1. **Измените все пароли по умолчанию**
2. **Настройте SSL сертификаты для HTTPS**
3. **Ограничьте доступ к портам базы данных**
4. **Используйте Docker secrets для чувствительных данных**
5. **Регулярно обновляйте образы**

### SSL сертификаты:

```bash
# Добавьте сертификаты в nginx.conf и раскомментируйте HTTPS секцию
# Поместите cert.pem и key.pem в директорию ssl/
```

## 🗄️ Резервное копирование

### База данных:
```bash
# Создание backup
docker-compose exec database pg_dump -U carebot carebot > backup.sql

# Восстановление из backup
docker-compose exec -T database psql -U carebot carebot < backup.sql
```

### Данные миссий:
```bash
# Backup missions data
docker cp carebot_webapp:/app/missions ./backup_missions/

# Restore missions data
docker cp ./backup_missions/ carebot_webapp:/app/missions
```

## 🧪 Разработка

### Разработка с Docker:

```bash
# Запуск только базы данных для локальной разработки
docker-compose up -d database redis

# Запуск приложения локально
python server_app.py
python handlers.py
```

### Отладка контейнера:

```bash
# Подключение к контейнеру
docker-compose exec webapp bash
docker-compose exec telegram_bot bash

# Просмотр файлов в контейнере
docker-compose exec webapp ls -la /app
```

## ❗ Устранение неполадок

### Общие проблемы:

1. **Контейнер не запускается**:
   ```bash
   docker-compose logs <service_name>
   ```

2. **База данных недоступна**:
   ```bash
   docker-compose logs database
   docker-compose restart database
   ```

3. **Telegram бот не отвечает**:
   - Проверьте TELEGRAM_BOT_TOKEN в .env
   - Проверьте логи: `docker-compose logs telegram_bot`

4. **Web приложение недоступно**:
   ```bash
   curl http://localhost/health
   docker-compose logs webapp
   docker-compose logs nginx
   ```

### Очистка системы:

```bash
# Остановка и удаление всех контейнеров
docker-compose down --volumes --remove-orphans

# Очистка всех Docker данных (ОСТОРОЖНО!)
docker system prune -a --volumes
```

## 📈 Производительность

### Рекомендации для продакшена:

1. **Ресурсы**: Минимум 2 CPU, 4GB RAM
2. **Диск**: SSD рекомендуется
3. **Масштабирование**: Используйте Docker Swarm или Kubernetes
4. **Load Balancer**: Nginx upstream для нескольких экземпляров webapp
5. **Кэширование**: Redis настроен и готов к использованию

### Мониторинг производительности:

```bash
# Статистика контейнеров
docker stats

# Использование диска
docker system df

# Анализ логов
docker-compose logs --tail=100 webapp | grep ERROR
```
