# 📱 Crusade Mobile & Print System

Система включает офлайн мобильное приложение, серверную синхронизацию и автоматическую печать миссий.

## 🏗️ Архитектура системы

### 1. **Mission Engine** (Общий модуль)
- `mission_engine/__init__.py` - Генератор миссий, работает как на сервере, так и на мобильном устройстве
- Поддерживает все типы игр: Kill Team, WH40k, Boarding Action, Combat Patrol, Battlefleet
- Автоматическая печать сгенерированных миссий

### 2. **Серверное приложение** 
- `server_app.py` - Flask сервер с API синхронизации
- `sync_api.py` - API для синхронизации с мобильными устройствами
- Веб-интерфейс для управления миссиями и печати

### 3. **Мобильное приложение**
- `mobile_app/crusade_mobile.py` - Офлайн приложение на Kivy
- Работает без интернета, синхронизируется по Wi-Fi
- Локальная SQLite база для кэширования данных

## 🚀 Установка и запуск

### Установка зависимостей:
```bash
pip install -r requirements.txt
```

### Запуск серверного приложения:
```bash
python server_app.py
```
Сервер будет доступен по адресу: `http://192.168.1.100:5000`

### Запуск мобильного приложения:

#### С Kivy (графический интерфейс):
```bash
cd mobile_app
python crusade_mobile.py
```

#### Консольный режим (если Kivy недоступен):
```bash
cd mobile_app
python crusade_mobile.py
```

## 📱 Функции мобильного приложения

### ✅ Офлайн работа:
- Генерация миссий без интернета
- Локальное хранение данных
- Кэширование карты и игроков

### 🔄 Синхронизация по Wi-Fi:
- Автоматическая синхронизация каждые 30 секунд
- Ручная синхронизация по требованию
- Очередь синхронизации для офлайн изменений

### 🎯 Генерация миссий:
- Поддержка всех типов миссий
- Выбор игроков и локации
- Планирование времени проведения

## 🖨️ Система печати

### Автоматическая печать:
- Печать сразу после генерации миссии
- Форматированный вывод для принтера
- Поддержка Windows, Linux, macOS

### Веб-станция печати:
- `http://server-ip:5000/print-station`
- Генерация и печать миссий через браузер
- Предварительный просмотр миссий

## 🌐 API Endpoints

### Синхронизация:
- `GET /api/ping` - проверка доступности сервера
- `POST /api/sync` - синхронизация данных
- `GET /api/map-data` - получение данных карты
- `GET /api/players` - список игроков

### Миссии:
- `GET /api/missions` - список активных миссий
- `GET /api/mission/<id>` - детали миссии
- `POST /api/generate-mission` - генерация новой миссии
- `POST /api/print-mission/<id>` - печать миссии

## 🔧 Настройка сети

### 1. Настройка сервера:
```python
# В server_app.py измените IP и порт:
app.run(host='192.168.1.100', port=5000)
```

### 2. Настройка мобильного приложения:
```python
# В mobile_app/crusade_mobile.py:
self.sync_url = "http://192.168.1.100:5000"
```

### 3. Настройка принтера:
- Windows: использует notepad для печати
- Linux: использует команду `lp`
- macOS: использует команду `lpr`

## 📊 Использование

### Типичный рабочий процесс:

1. **Подготовка игры:**
   - Запустите сервер
   - Откройте мобильное приложение
   - Убедитесь в синхронизации

2. **Генерация миссии:**
   - Выберите тип миссии
   - Укажите игроков
   - Выберите hex на карте
   - Сгенерируйте и распечатайте миссию

3. **Проведение игры:**
   - Игроки используют распечатанную миссию
   - Результаты вводятся в систему
   - Карта обновляется автоматически

4. **Синхронизация:**
   - Результаты синхронизируются с сервером
   - Обновления распространяются на все устройства

## 🗂️ Структура файлов

```
CareBot/
├── mission_engine/
│   └── __init__.py          # Общий движок миссий
├── mobile_app/
│   └── crusade_mobile.py    # Мобильное приложение
├── templates/
│   ├── missions.html        # Управление миссиями
│   ├── print_station.html   # Станция печати
│   └── map.html            # Карта (Mini App)
├── server_app.py           # Основной сервер
├── sync_api.py             # API синхронизации
└── requirements.txt        # Зависимости
```

## 📋 Примеры использования

### Генерация миссии в консоли:
```bash
python -c "
from mission_engine import MissionGenerator, MissionType, Player, MapHex, Faction
gen = MissionGenerator()
players = [Player('1', 'John', Faction.SPACE_MARINES, 'alliance_1')]
hex = MapHex(1, 1, 'contested')
mission = gen.generate_mission(MissionType.KILL_TEAM, players, hex)
print(mission.description)
"
```

### Проверка синхронизации:
```bash
curl http://192.168.1.100:5000/api/ping
```

### Получение данных карты:
```bash
curl http://192.168.1.100:5000/api/map-data
```

## 🔍 Отладка

### Логи мобильного приложения:
- Консольные сообщения о статусе синхронизации
- Ошибки сохранения в локальную БД

### Логи сервера:
- Запросы синхронизации в консоли Flask
- Ошибки API в логах сервера

### Проверка печати:
- Файлы миссий сохраняются как `mission_*.txt`
- Проверьте настройки принтера по умолчанию

## 🚨 Устранение неполадок

### Проблемы синхронизации:
1. Проверьте сетевое подключение
2. Убедитесь, что сервер запущен
3. Проверьте IP-адрес в настройках

### Проблемы печати:
1. Проверьте подключение принтера
2. Убедитесь, что принтер установлен по умолчанию
3. Проверьте файлы в папке проекта

### Проблемы с Kivy:
```bash
# Если Kivy не установлен:
pip install kivy[base]

# Для Linux дополнительно:
sudo apt-get install python3-kivy
```

Система готова к использованию! 🎉
